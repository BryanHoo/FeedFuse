# AI Digest Sources Implementation Plan

> **For agentic workers:** REQUIRED: Use workflow-subagent-driven-development (if subagents available) or workflow-executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 FeedFuse 中新增 `AI解读源`（`feeds.kind='ai_digest'`），支持“间隔语义”的定时生成与手动“立即生成”，每次触发在窗口内有更新时生成 **一篇新的** 解读文章（累积成列表），并确保解读文章 **不会出现在** `all/unread/starred` 智能视图与“全局一键已读”中。

**Architecture:** 采用方案 B：`AI解读源` 作为 `feeds.kind='ai_digest'` 的一种 feed 类型存在；生成结果复用 `articles`（`articles.feed_id = ai_digest_feed_id`），智能视图在查询与操作层统一限定 `feeds.kind='rss'`。后台基于 `pg-boss` 新增 `ai.digest_tick`（每分钟扫描 due config）与 `ai.digest_generate`（执行一次 run：筛选候选、生成 HTML、落库、推进窗口游标）。

**Tech Stack:** Next.js Route Handlers, TypeScript, PostgreSQL, `pg-boss`, Vitest, Zustand, Radix UI, `openai` (OpenAI-compatible API), `sanitize-html`

---

## Context To Carry Forward

- 已确认 spec：`docs/superpowers/specs/2026-03-14-ai-digest-design.md`。实现不得偏离这些硬约束：
  - `AI解读源` 在左栏表现为“订阅源条目”，与 RSS 源同级（不是分类节点）。
  - 每次触发生成 **一篇新的** 解读文章（累积），不是覆盖同一篇。
  - 时间窗口以 `articles.fetched_at` 判定（不是 `published_at`）；窗口为 `(last_window_end_at, now]`。
  - 首次窗口从创建时刻开始（`ai_digest_configs.last_window_end_at = now()`），不回看历史。
  - 窗口内无新文章则 **不生成文章**，但必须推进 `last_window_end_at`（避免重复扫同一窗口）。
  - 重复时间为“间隔语义”，UI 使用固定选项（非自由输入）。
  - 来源可多选 RSS feeds 与分类；选择分类时运行时动态解析该分类下 RSS feeds，且明确 **不包括 `AI解读源`**（靠 `feeds.kind='rss'` 过滤）。
  - 解读文章 **不应出现在** `全部/未读/收藏` 智能视图里（只在该 `AI解读源` 下可见）。
  - 在该 `AI解读源` view 下，把“刷新订阅源”按钮替换为“立即生成”（手动触发语义与自动一致）。
  - 解读标题由 AI 生成；失败兜底用 `feeds.title`。
  - 输入策略：优先 `content_full_html`，否则 `title + summary`；不会为了解读额外抓全文。
  - 相关性策略：先筛选再解读，`Top N = 10`；筛选失败降级为按 `fetched_at desc` 取最新 10 篇。
- 现有三栏阅读器 snapshot 由 `src/server/services/readerSnapshotService.ts` 生成，智能视图过滤目前只在 `articles` 上做 where；本功能必须把“智能视图隔离”补齐（查询 + 操作两类）。
- 后台任务基于 `pg-boss`：主入口 `src/worker/index.ts`，队列 contract 在 `src/server/queue/contracts.ts`，job 常量在 `src/server/queue/jobs.ts`。
- AI key 与模型配置来自 DB：`getAiApiKey()` + `normalizePersistedSettings()`（`ai.model`、`ai.apiBaseUrl`）。
- HTML 清洗复用：`src/server/rss/sanitizeContent.ts`。
- repo 当前没有与 “ai digest” 相关的 `docs/summaries/` 记录（已全局 grep 无匹配）。
- 参考技能（仅作为执行建议）：`@vitest`（测试拆分与 mock）、`@postgresql-table-design`（表结构/索引/约束）、`@nodejs-best-practices`（服务端边界与可测试性）。

## File Map

### Create

- `src/server/db/migrations/0019_ai_digest_sources.sql`
  - 新增 `feeds.kind` + `ai_digest_configs` + `ai_digest_runs` + 索引
- `src/server/db/migrations/aiDigestSourcesMigration.test.ts`
  - migration 内容断言
- `src/server/repositories/aiDigestRepo.ts`
  - `ai_digest_configs` / `ai_digest_runs` 的读写（含 due 查询、候选查询）
- `src/server/repositories/aiDigestRepo.test.ts`
  - repo SQL 形状断言（不接真实 DB）
- `src/server/services/aiDigestLifecycleService.ts`
  - 创建 `AI解读源`（事务：分类解析 + feed + config）
- `src/server/services/aiDigestLifecycleService.test.ts`
  - service 行为 contract（mock repos）
- `src/app/api/ai-digests/route.ts`
  - `POST /api/ai-digests` 创建 `AI解读源`
- `src/app/api/ai-digests/[feedId]/generate/route.ts`
  - `POST /api/ai-digests/:feedId/generate` 手动触发生成（创建 run + enqueue）
- `src/app/api/ai-digests/routes.test.ts`
  - route contract 测试（mock pool / repos / queue）
- `src/server/ai/aiDigestRerank.ts`
  - “增量 shortlist rerank” LLM helper（可单测）
- `src/server/ai/aiDigestRerank.test.ts`
- `src/server/ai/aiDigestCompose.ts`
  - Map-Reduce + 递归折叠，输出 `{ title, html }`
- `src/server/ai/aiDigestCompose.test.ts`
- `src/worker/aiDigestTick.ts`
  - tick 编排（scan due configs -> create run -> enqueue）
- `src/worker/aiDigestTick.test.ts`
- `src/worker/aiDigestGenerate.ts`
  - generate 编排（resolve feeds -> query candidates -> rerank -> compose -> sanitize -> insert -> advance cursor）
- `src/worker/aiDigestGenerate.test.ts`
- `src/features/feeds/AddAiDigestDialog.tsx`
  - wrapper（类似 `AddFeedDialog.tsx`）
- `src/features/feeds/AiDigestDialog.tsx`
  - modal shell + 表单组合
- `src/features/feeds/AiDigestDialogForm.tsx`
  - 纯表单 UI（输入、校验提示、来源多选、间隔选择、分类选择）
- `src/features/feeds/useAiDigestDialogForm.ts`
  - 表单状态、提交、错误处理
- `src/features/feeds/AddAiDigestDialog.test.tsx`
  - 组件级交互测试（中文可访问名称 + 必填校验）

### Modify

- `src/server/repositories/feedsRepo.ts`
  - `FeedRow` 增加 `kind`，`listFeeds()`/select 返回 `kind`
  - `listEnabledFeedsForFetch()` / `getFeedForFetch()` / `updateAllFeedsFetchIntervalMinutes()` 仅处理 `kind='rss'`
  - 增加 `createAiDigestFeed()`（插入 `kind='ai_digest'` + 合成 URL）
- `src/server/repositories/articlesRepo.ts`
  - `markAllRead()`：当未指定 `feedId` 时仅作用于 `feeds.kind='rss'`
- `src/server/repositories/articlesRepo.markAllRead.test.ts`
  - 新增：markAllRead 的 SQL 形状断言
- `src/server/repositories/feedsRepo.kind.test.ts`
  - 新增：feed.kind 与 RSS-only fetch 过滤的 SQL 断言
- `src/server/services/readerSnapshotService.ts`
  - `ReaderSnapshotFeed` 增加 `kind`
  - `buildArticleFilter()`：智能视图追加 `feeds.kind='rss'` 过滤
- `src/server/services/readerSnapshotService.test.ts`
  - 更新断言：`unread/starred/all` 必须包含 rss-only 过滤；feed view 不包含
- `src/server/queue/jobs.ts`
  - 新增 `JOB_AI_DIGEST_TICK` / `JOB_AI_DIGEST_GENERATE`
- `src/server/queue/jobs.test.ts`
  - job 名稳定性断言
- `src/server/queue/contracts.ts`
  - 新增两条 job contract（并扩展 `SendContext` 支持 `runId`）
- `src/worker/index.ts`
  - 注册新 workers + `boss.schedule(JOB_AI_DIGEST_TICK, '* * * * *')`
- `src/types/index.ts`
  - `Feed` 增加 `kind: 'rss' | 'ai_digest'`
- `src/lib/apiClient.ts`
  - Feed DTO 增加 `kind`
  - 新增 `createAiDigest()` / `generateAiDigest()` helper
- `src/lib/apiClient.test.ts`
- `src/store/appStore.ts`
  - 新增 `addAiDigest()`（创建后只 `loadSnapshot`，不触发 `refreshFeed`）
- `src/features/feeds/FeedList.tsx`
  - `+` 按钮改为菜单：`添加 RSS 源` / `添加 AI解读`
  - context menu：对 `ai_digest` 隐藏 RSS 专属项（编辑/全文抓取/AI摘要/翻译/关键词过滤）
- `src/features/feeds/FeedList.test.tsx`
- `src/features/articles/ArticleList.tsx`
  - `ai_digest` feed view：工具栏用 `立即生成` 替代 `刷新订阅源`
- `src/features/articles/ArticleList.test.tsx`

### Reuse Without Modification

- `src/server/rss/sanitizeContent.ts`
- `src/server/repositories/settingsRepo.ts`（`getAiApiKey()`、`getUiSettings()`）
- `src/features/feeds/CreatableCategoryField.tsx`（分类选择/创建）
- `src/server/http/apiResponse.ts` / `src/server/http/errors.ts`

---

## Chunk 1: Schema + Feed Kind + 智能视图隔离

### Task 1: 数据库迁移（`feeds.kind` + `ai_digest_*` 表 + 索引）

**Files:**

- Create: `src/server/db/migrations/0019_ai_digest_sources.sql`
- Test: `src/server/db/migrations/aiDigestSourcesMigration.test.ts`

- [ ] **Step 1: 先写 migration test（应当失败）**

```ts
import { existsSync, readFileSync } from 'node:fs';
import { describe, expect, it } from 'vitest';

describe('db migrations', () => {
  it('adds ai_digest feed kind and digest tables', () => {
    const migrationPath = 'src/server/db/migrations/0019_ai_digest_sources.sql';
    expect(existsSync(migrationPath)).toBe(true);

    const sql = readFileSync(migrationPath, 'utf8');
    expect(sql).toContain("add column if not exists kind");
    expect(sql).toContain("default 'rss'");
    expect(sql).toContain("create table if not exists ai_digest_configs");
    expect(sql).toContain("create table if not exists ai_digest_runs");
    expect(sql).toContain("create index if not exists articles_feed_fetched_id_idx");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test:unit src/server/db/migrations/aiDigestSourcesMigration.test.ts -t "adds ai_digest feed kind and digest tables"`

Expected: FAIL（`existsSync(...)` 断言失败）

- [ ] **Step 3: 添加 migration SQL**

```sql
alter table feeds
  add column if not exists kind text not null default 'rss';

alter table feeds
  add constraint if not exists feeds_kind_check
    check (kind in ('rss', 'ai_digest'));

create table if not exists ai_digest_configs (
  feed_id uuid primary key references feeds(id) on delete cascade,
  prompt text not null,
  interval_minutes int not null,
  top_n int not null default 10,
  selected_feed_ids uuid[] not null default '{}'::uuid[],
  selected_category_ids uuid[] not null default '{}'::uuid[],
  last_window_end_at timestamptz not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  constraint ai_digest_configs_interval_minutes_positive check (interval_minutes > 0),
  constraint ai_digest_configs_top_n_positive check (top_n > 0)
);

create table if not exists ai_digest_runs (
  id uuid primary key default gen_random_uuid(),
  feed_id uuid not null references feeds(id) on delete cascade,
  window_start_at timestamptz not null,
  window_end_at timestamptz not null,
  status text not null,
  candidate_total int not null default 0,
  selected_count int not null default 0,
  article_id uuid null references articles(id),
  model text null,
  error_code text null,
  error_message text null,
  job_id text null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  constraint ai_digest_runs_status_check check (
    status in ('queued', 'running', 'succeeded', 'failed', 'skipped_no_updates')
  )
);

create unique index if not exists ai_digest_runs_feed_window_unique
  on ai_digest_runs(feed_id, window_start_at);

create index if not exists articles_feed_fetched_id_idx
  on articles(feed_id, fetched_at desc, id desc);
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test:unit src/server/db/migrations/aiDigestSourcesMigration.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/server/db/migrations/0019_ai_digest_sources.sql src/server/db/migrations/aiDigestSourcesMigration.test.ts
git commit -m "feat(db): 添加 AI解读源表结构" \
  -m "- 添加 feeds.kind 与 ai_digest_configs/ai_digest_runs" \
  -m "- 添加 articles(feed_id,fetched_at,id) 索引"
```

### Task 2: `feedsRepo` 支持 `kind` + RSS 抓取逻辑只处理 `rss`

**Files:**

- Modify: `src/server/repositories/feedsRepo.ts`
- Create: `src/server/repositories/feedsRepo.kind.test.ts`

- [ ] **Step 1: 先写失败测试：`listFeeds` 必须 select `kind`，抓取相关 query 必须限定 `kind='rss'`**

```ts
import { describe, expect, it, vi } from 'vitest';
import type { Pool } from 'pg';

describe('feedsRepo (kind)', () => {
  it('listFeeds selects kind', async () => {
    const query = vi.fn().mockResolvedValue({ rows: [] });
    const pool = { query } as unknown as Pool;
    const mod = (await import('./feedsRepo')) as typeof import('./feedsRepo');

    await mod.listFeeds(pool);
    const sql = String(query.mock.calls[0]?.[0] ?? '');
    expect(sql).toContain('kind');
  });

  it('rss fetch helpers only select rss feeds', async () => {
    const query = vi.fn().mockResolvedValue({ rows: [] });
    const pool = { query } as unknown as Pool;
    const mod = (await import('./feedsRepo')) as typeof import('./feedsRepo');

    await mod.listEnabledFeedsForFetch(pool);
    const listSql = String(query.mock.calls[0]?.[0] ?? '');
    expect(listSql).toContain(\"kind = 'rss'\");

    query.mockClear();
    await mod.getFeedForFetch(pool, 'feed-1');
    const getSql = String(query.mock.calls[0]?.[0] ?? '');
    expect(getSql).toContain(\"kind = 'rss'\");
  });

  it('updateAllFeedsFetchIntervalMinutes only updates rss feeds', async () => {
    const query = vi.fn().mockResolvedValue({ rowCount: 0 });
    const pool = { query } as unknown as Pool;
    const mod = (await import('./feedsRepo')) as typeof import('./feedsRepo');

    await mod.updateAllFeedsFetchIntervalMinutes(pool, 30);
    const sql = String(query.mock.calls[0]?.[0] ?? '');
    expect(sql).toContain(\"where kind = 'rss'\");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test:unit src/server/repositories/feedsRepo.kind.test.ts`

Expected: FAIL（SQL 断言不满足 / `FeedRow` 不含 `kind`）

- [ ] **Step 3: 实现：为 `FeedRow`/select/returning 增加 `kind`，并把抓取相关函数限定为 `rss`**

在 `src/server/repositories/feedsRepo.ts`：

- `FeedRow` 增加：

```ts
export type FeedKind = 'rss' | 'ai_digest';

export interface FeedRow {
  // ...
  kind: FeedKind;
}
```

- `listFeeds()` 的 select/returning 增加 `kind`
- `createFeed()`/`updateFeed()` 的 returning 增加 `kind`（不需要允许 patch `kind`）
- `listEnabledFeedsForFetch()` 增加 where：`where enabled = true and kind = 'rss'`
- `getFeedForFetch()` 增加 where：`where id = $1 and kind = 'rss'`
- `updateAllFeedsFetchIntervalMinutes()` 增加 where：`where kind = 'rss'`
- 新增 `createAiDigestFeed()`（用于 `POST /api/ai-digests`）

```ts
export async function createAiDigestFeed(
  db: DbClient,
  input: { id: string; title: string; categoryId: string | null },
): Promise<FeedRow> {
  const url = `http://localhost/__feedfuse_ai_digest__/${input.id}`;
  const { rows } = await db.query<FeedRow>(
    `
      insert into feeds(id, kind, title, url, category_id)
      values ($1, 'ai_digest', $2, $3, $4)
      returning
        id,
        kind,
        title,
        url,
        site_url as "siteUrl",
        icon_url as "iconUrl",
        enabled,
        full_text_on_open_enabled as "fullTextOnOpenEnabled",
        ai_summary_on_open_enabled as "aiSummaryOnOpenEnabled",
        ai_summary_on_fetch_enabled as "aiSummaryOnFetchEnabled",
        body_translate_on_fetch_enabled as "bodyTranslateOnFetchEnabled",
        body_translate_on_open_enabled as "bodyTranslateOnOpenEnabled",
        title_translate_enabled as "titleTranslateEnabled",
        body_translate_enabled as "bodyTranslateEnabled",
        article_list_display_mode as "articleListDisplayMode",
        category_id as "categoryId",
        fetch_interval_minutes as "fetchIntervalMinutes",
        last_fetch_status as "lastFetchStatus",
        last_fetch_error as "lastFetchError"
    `,
    [input.id, input.title, url, input.categoryId],
  );
  return rows[0];
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test:unit src/server/repositories/feedsRepo.kind.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/server/repositories/feedsRepo.ts src/server/repositories/feedsRepo.kind.test.ts
git commit -m "refactor(feed): 引入 feeds.kind 并隔离 RSS 抓取" \
  -m "- 添加 FeedRow.kind 并在 listFeeds/create/update 返回" \
  -m "- 限定抓取与全局抓取间隔只处理 kind='rss'"
```

### Task 3: 智能视图查询隔离（`buildArticleFilter` 追加 rss-only 条件）

**Files:**

- Modify: `src/server/services/readerSnapshotService.ts` (见 `buildArticleFilter()`，当前约在 `:41`)
- Modify: `src/server/services/readerSnapshotService.test.ts`

- [ ] **Step 1: 先写失败测试：智能视图必须包含 rss-only 过滤**

在 `src/server/services/readerSnapshotService.test.ts` 增加/更新断言：

```ts
import { describe, expect, it } from 'vitest';
import { buildArticleFilter } from './readerSnapshotService';

const RSS_ONLY = "feed_id in (select id from feeds where kind = 'rss')";

describe('readerSnapshotService', () => {
  it('filters unread view and excludes ai_digest', () => {
    const filter = buildArticleFilter({ view: 'unread' });
    expect(filter.whereSql).toMatch(/is_read = false/);
    expect(filter.whereSql).toContain(RSS_ONLY);
  });

  it('filters starred view and excludes ai_digest', () => {
    const filter = buildArticleFilter({ view: 'starred' });
    expect(filter.whereSql).toMatch(/is_starred = true/);
    expect(filter.whereSql).toContain(RSS_ONLY);
  });

  it('filters all view and excludes ai_digest', () => {
    const filter = buildArticleFilter({ view: 'all' });
    expect(filter.whereSql).toContain(RSS_ONLY);
  });

  it('does not force rss-only when viewing a specific feedId', () => {
    const filter = buildArticleFilter({ view: 'feed-id-1' });
    expect(filter.whereSql).toMatch(/feed_id/);
    expect(filter.whereSql).not.toContain(RSS_ONLY);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test:unit src/server/services/readerSnapshotService.test.ts`

Expected: FAIL（`whereSql` 不包含 rss-only 条件）

- [ ] **Step 3: 实现：对 `all/unread/starred` 追加 `feeds.kind='rss'` 子查询过滤**

在 `src/server/services/readerSnapshotService.ts` 的 `buildArticleFilter()` 里（当前约 `:41`）：

```ts
  const isSmartView =
    input.view === 'all' || input.view === 'unread' || input.view === 'starred';

  if (isSmartView) {
    whereParts.push("feed_id in (select id from feeds where kind = 'rss')");
  }
```

同时把 `ReaderSnapshotFeed` 增加 `kind: 'rss' | 'ai_digest'`，保证前端可基于 kind 切换“刷新订阅源 / 立即生成”按钮：

```ts
export interface ReaderSnapshotFeed {
  // ...
  kind: 'rss' | 'ai_digest';
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test:unit src/server/services/readerSnapshotService.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/server/services/readerSnapshotService.ts src/server/services/readerSnapshotService.test.ts
git commit -m "fix(reader): 智能视图排除 AI解读源文章" \
  -m "- 在 buildArticleFilter 为 all/unread/starred 追加 kind='rss' 过滤" \
  -m "- 为 ReaderSnapshotFeed 暴露 feed.kind"
```

### Task 4: 智能视图操作隔离（`markAllRead` 默认只作用于 rss）

**Files:**

- Modify: `src/server/repositories/articlesRepo.ts` (当前 `markAllRead` 约在 `:202`)
- Create: `src/server/repositories/articlesRepo.markAllRead.test.ts`

- [ ] **Step 1: 先写失败测试：不带 `feedId` 时必须注入 rss-only 条件**

```ts
import { describe, expect, it, vi } from 'vitest';
import type { Pool } from 'pg';

describe('articlesRepo (markAllRead)', () => {
  it('filters rss only when feedId is omitted', async () => {
    const query = vi.fn().mockResolvedValue({ rowCount: 0 });
    const pool = { query } as unknown as Pool;
    const mod = (await import('./articlesRepo')) as typeof import('./articlesRepo');

    await mod.markAllRead(pool, {});
    const sql = String(query.mock.calls[0]?.[0] ?? '');
    expect(sql).toContain("feed_id in (select id from feeds where kind = 'rss')");
  });

  it('does not inject rss-only when feedId is provided', async () => {
    const query = vi.fn().mockResolvedValue({ rowCount: 0 });
    const pool = { query } as unknown as Pool;
    const mod = (await import('./articlesRepo')) as typeof import('./articlesRepo');

    await mod.markAllRead(pool, { feedId: 'feed-1' });
    const sql = String(query.mock.calls[0]?.[0] ?? '');
    expect(sql).not.toContain("select id from feeds where kind = 'rss'");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test:unit src/server/repositories/articlesRepo.markAllRead.test.ts`

Expected: FAIL（SQL 不包含 rss-only 条件）

- [ ] **Step 3: 实现：当 `feedId` 缺省时限定 `feed_id in (select id from feeds where kind='rss')`**

在 `src/server/repositories/articlesRepo.ts` 的 `markAllRead()`：

```ts
  if (input.feedId) {
    params.push(`feed_id = $${index++}`);
    values.push(input.feedId);
  } else {
    params.push("feed_id in (select id from feeds where kind = 'rss')");
  }
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test:unit src/server/repositories/articlesRepo.markAllRead.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/server/repositories/articlesRepo.ts src/server/repositories/articlesRepo.markAllRead.test.ts
git commit -m "fix(article): 全局一键已读仅作用于 RSS" \
  -m "- 在 markAllRead 缺省 feedId 时过滤 kind='rss'" \
  -m "- 保留按 feedId 标记已读的原有语义"
```

---

## Chunk 2: Digest 配置/运行数据层 + API（创建 & 立即生成）

### Task 5: 新增 `aiDigestRepo`（configs/runs/due/candidates）

**Files:**

- Create: `src/server/repositories/aiDigestRepo.ts`
- Test: `src/server/repositories/aiDigestRepo.test.ts`

- [ ] **Step 1: 先写失败测试：repo 暴露必要函数且 SQL 包含关键过滤**

```ts
import { describe, expect, it, vi } from 'vitest';
import type { Pool } from 'pg';

describe('aiDigestRepo', () => {
  it('lists due configs only for enabled ai_digest feeds', async () => {
    const query = vi.fn().mockResolvedValue({ rows: [] });
    const pool = { query } as unknown as Pool;
    const mod = (await import('./aiDigestRepo')) as typeof import('./aiDigestRepo');

    await mod.listDueAiDigestConfigFeedIds(pool, { now: new Date('2026-03-14T00:00:00.000Z') });
    const sql = String(query.mock.calls[0]?.[0] ?? '');
    expect(sql).toContain('from ai_digest_configs');
    expect(sql).toContain('join feeds');
    expect(sql).toContain("feeds.kind = 'ai_digest'");
    expect(sql).toContain('feeds.enabled = true');
  });

  it('queries candidate articles by fetched_at window', async () => {
    const query = vi.fn().mockResolvedValue({ rows: [] });
    const pool = { query } as unknown as Pool;
    const mod = (await import('./aiDigestRepo')) as typeof import('./aiDigestRepo');

    await mod.listAiDigestCandidateArticles(pool, {
      targetFeedIds: ['00000000-0000-0000-0000-000000000000'],
      windowStartAt: '2026-03-14T00:00:00.000Z',
      windowEndAt: '2026-03-14T01:00:00.000Z',
      limit: 500,
    });
    const sql = String(query.mock.calls[0]?.[0] ?? '');
    expect(sql).toContain('from articles');
    expect(sql).toContain('fetched_at');
    expect(sql).toContain('> $');
    expect(sql).toContain('<= $');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test:unit src/server/repositories/aiDigestRepo.test.ts`

Expected: FAIL（module 不存在 / 函数未导出）

- [ ] **Step 3: 实现 `aiDigestRepo.ts`（建议直接复制下方完整实现）**

```ts
import type { Pool, PoolClient } from 'pg';

type DbClient = Pool | PoolClient;

export type AiDigestRunStatus =
  | 'queued'
  | 'running'
  | 'succeeded'
  | 'failed'
  | 'skipped_no_updates';

export interface AiDigestConfigRow {
  feedId: string;
  prompt: string;
  intervalMinutes: number;
  topN: number;
  selectedFeedIds: string[];
  selectedCategoryIds: string[];
  lastWindowEndAt: string;
  createdAt: string;
  updatedAt: string;
}

export interface AiDigestRunRow {
  id: string;
  feedId: string;
  windowStartAt: string;
  windowEndAt: string;
  status: AiDigestRunStatus;
  candidateTotal: number;
  selectedCount: number;
  articleId: string | null;
  model: string | null;
  errorCode: string | null;
  errorMessage: string | null;
  jobId: string | null;
  createdAt: string;
  updatedAt: string;
}

export async function createAiDigestConfig(
  db: DbClient,
  input: {
    feedId: string;
    prompt: string;
    intervalMinutes: number;
    topN?: number;
    selectedFeedIds: string[];
    selectedCategoryIds: string[];
    lastWindowEndAt: string;
  },
): Promise<AiDigestConfigRow> {
  const { rows } = await db.query<AiDigestConfigRow>(
    `
      insert into ai_digest_configs(
        feed_id,
        prompt,
        interval_minutes,
        top_n,
        selected_feed_ids,
        selected_category_ids,
        last_window_end_at
      )
      values ($1, $2, $3, $4, $5::uuid[], $6::uuid[], $7::timestamptz)
      returning
        feed_id as "feedId",
        prompt,
        interval_minutes as "intervalMinutes",
        top_n as "topN",
        selected_feed_ids as "selectedFeedIds",
        selected_category_ids as "selectedCategoryIds",
        last_window_end_at as "lastWindowEndAt",
        created_at as "createdAt",
        updated_at as "updatedAt"
    `,
    [
      input.feedId,
      input.prompt,
      input.intervalMinutes,
      input.topN ?? 10,
      input.selectedFeedIds,
      input.selectedCategoryIds,
      input.lastWindowEndAt,
    ],
  );
  return rows[0];
}

export async function getAiDigestConfigByFeedId(
  db: DbClient,
  feedId: string,
): Promise<AiDigestConfigRow | null> {
  const { rows } = await db.query<AiDigestConfigRow>(
    `
      select
        feed_id as "feedId",
        prompt,
        interval_minutes as "intervalMinutes",
        top_n as "topN",
        selected_feed_ids as "selectedFeedIds",
        selected_category_ids as "selectedCategoryIds",
        last_window_end_at as "lastWindowEndAt",
        created_at as "createdAt",
        updated_at as "updatedAt"
      from ai_digest_configs
      where feed_id = $1
      limit 1
    `,
    [feedId],
  );
  return rows[0] ?? null;
}

export async function updateAiDigestConfigLastWindowEndAt(
  db: DbClient,
  feedId: string,
  lastWindowEndAt: string,
): Promise<void> {
  await db.query(
    `
      update ai_digest_configs
      set
        last_window_end_at = $2::timestamptz,
        updated_at = now()
      where feed_id = $1
    `,
    [feedId, lastWindowEndAt],
  );
}

export async function listDueAiDigestConfigFeedIds(
  db: DbClient,
  input: { now: Date },
): Promise<string[]> {
  const { rows } = await db.query<{ feedId: string }>(
    `
      select c.feed_id as "feedId"
      from ai_digest_configs c
      join feeds on feeds.id = c.feed_id
      where
        feeds.kind = 'ai_digest'
        and feeds.enabled = true
        and c.last_window_end_at <= ($1::timestamptz - (c.interval_minutes * interval '1 minute'))
      order by c.last_window_end_at asc, c.feed_id asc
    `,
    [input.now.toISOString()],
  );
  return rows.map((row) => row.feedId);
}

export async function getAiDigestRunByFeedIdAndWindowStartAt(
  db: DbClient,
  input: { feedId: string; windowStartAt: string },
): Promise<AiDigestRunRow | null> {
  const { rows } = await db.query<AiDigestRunRow>(
    `
      select
        id,
        feed_id as "feedId",
        window_start_at as "windowStartAt",
        window_end_at as "windowEndAt",
        status,
        candidate_total as "candidateTotal",
        selected_count as "selectedCount",
        article_id as "articleId",
        model,
        error_code as "errorCode",
        error_message as "errorMessage",
        job_id as "jobId",
        created_at as "createdAt",
        updated_at as "updatedAt"
      from ai_digest_runs
      where feed_id = $1 and window_start_at = $2::timestamptz
      limit 1
    `,
    [input.feedId, input.windowStartAt],
  );
  return rows[0] ?? null;
}

export async function getAiDigestRunById(
  db: DbClient,
  runId: string,
): Promise<AiDigestRunRow | null> {
  const { rows } = await db.query<AiDigestRunRow>(
    `
      select
        id,
        feed_id as "feedId",
        window_start_at as "windowStartAt",
        window_end_at as "windowEndAt",
        status,
        candidate_total as "candidateTotal",
        selected_count as "selectedCount",
        article_id as "articleId",
        model,
        error_code as "errorCode",
        error_message as "errorMessage",
        job_id as "jobId",
        created_at as "createdAt",
        updated_at as "updatedAt"
      from ai_digest_runs
      where id = $1
      limit 1
    `,
    [runId],
  );
  return rows[0] ?? null;
}

export async function createAiDigestRun(
  db: DbClient,
  input: {
    feedId: string;
    windowStartAt: string;
    windowEndAt: string;
    status: AiDigestRunStatus;
  },
): Promise<AiDigestRunRow | null> {
  const { rows } = await db.query<AiDigestRunRow>(
    `
      insert into ai_digest_runs(feed_id, window_start_at, window_end_at, status)
      values ($1, $2::timestamptz, $3::timestamptz, $4)
      on conflict (feed_id, window_start_at) do nothing
      returning
        id,
        feed_id as "feedId",
        window_start_at as "windowStartAt",
        window_end_at as "windowEndAt",
        status,
        candidate_total as "candidateTotal",
        selected_count as "selectedCount",
        article_id as "articleId",
        model,
        error_code as "errorCode",
        error_message as "errorMessage",
        job_id as "jobId",
        created_at as "createdAt",
        updated_at as "updatedAt"
    `,
    [input.feedId, input.windowStartAt, input.windowEndAt, input.status],
  );
  return rows[0] ?? null;
}

export async function updateAiDigestRun(
  db: DbClient,
  runId: string,
  patch: Partial<{
    status: AiDigestRunStatus;
    candidateTotal: number;
    selectedCount: number;
    articleId: string | null;
    model: string | null;
    errorCode: string | null;
    errorMessage: string | null;
    jobId: string | null;
  }>,
): Promise<void> {
  const fields: string[] = [];
  const values: Array<string | number | null> = [];
  let index = 1;

  if (typeof patch.status !== 'undefined') {
    fields.push(`status = $${index++}`);
    values.push(patch.status);
  }
  if (typeof patch.candidateTotal !== 'undefined') {
    fields.push(`candidate_total = $${index++}`);
    values.push(patch.candidateTotal);
  }
  if (typeof patch.selectedCount !== 'undefined') {
    fields.push(`selected_count = $${index++}`);
    values.push(patch.selectedCount);
  }
  if (typeof patch.articleId !== 'undefined') {
    fields.push(`article_id = $${index++}`);
    values.push(patch.articleId);
  }
  if (typeof patch.model !== 'undefined') {
    fields.push(`model = $${index++}`);
    values.push(patch.model);
  }
  if (typeof patch.errorCode !== 'undefined') {
    fields.push(`error_code = $${index++}`);
    values.push(patch.errorCode);
  }
  if (typeof patch.errorMessage !== 'undefined') {
    fields.push(`error_message = $${index++}`);
    values.push(patch.errorMessage);
  }
  if (typeof patch.jobId !== 'undefined') {
    fields.push(`job_id = $${index++}`);
    values.push(patch.jobId);
  }

  if (fields.length === 0) return;

  fields.push('updated_at = now()');
  values.push(runId);

  await db.query(
    `
      update ai_digest_runs
      set ${fields.join(', ')}
      where id = $${index}
    `,
    values,
  );
}

export interface AiDigestCandidateArticleRow {
  id: string;
  feedId: string;
  feedTitle: string;
  title: string;
  summary: string | null;
  link: string | null;
  fetchedAt: string;
  contentFullHtml: string | null;
}

export async function listAiDigestCandidateArticles(
  db: DbClient,
  input: {
    targetFeedIds: string[];
    windowStartAt: string;
    windowEndAt: string;
    limit: number;
  },
): Promise<AiDigestCandidateArticleRow[]> {
  if (input.targetFeedIds.length === 0) return [];

  const { rows } = await db.query<AiDigestCandidateArticleRow>(
    `
      select
        a.id,
        a.feed_id as "feedId",
        f.title as "feedTitle",
        a.title,
        a.summary,
        a.link,
        a.fetched_at as "fetchedAt",
        a.content_full_html as "contentFullHtml"
      from articles a
      join feeds f on f.id = a.feed_id
      where
        a.feed_id = any($1::uuid[])
        and a.fetched_at > $2::timestamptz
        and a.fetched_at <= $3::timestamptz
      order by a.fetched_at desc, a.id desc
      limit $4
    `,
    [input.targetFeedIds, input.windowStartAt, input.windowEndAt, input.limit],
  );
  return rows;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test:unit src/server/repositories/aiDigestRepo.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/server/repositories/aiDigestRepo.ts src/server/repositories/aiDigestRepo.test.ts
git commit -m "feat(ai-digest): 添加解读配置与运行仓储层" \
  -m "- 添加 ai_digest_configs/ai_digest_runs 的 CRUD 与 due 查询" \
  -m "- 添加按 fetched_at 窗口读取候选文章查询"
```

### Task 6: 新增 `aiDigestLifecycleService`（创建 feed + config，复用分类解析规则）

**Files:**

- Create: `src/server/services/aiDigestLifecycleService.ts`
- Test: `src/server/services/aiDigestLifecycleService.test.ts`

- [ ] **Step 1: 先写失败测试：当 categoryName 命中既有分类时复用 id；否则创建**

（用 mock repo 的方式即可，参考 `src/server/services/feedCategoryLifecycleService.ts` 的测试风格）

```ts
import { describe, expect, it, vi } from 'vitest';

const connectMock = vi.fn();
const queryMock = vi.fn();
const releaseMock = vi.fn();

vi.mock('../repositories/categoriesRepo', () => ({
  findCategoryByNormalizedName: vi.fn(),
  getNextCategoryPosition: vi.fn(),
  createCategory: vi.fn(),
}));
vi.mock('../repositories/feedsRepo', () => ({
  createAiDigestFeed: vi.fn(),
}));
vi.mock('../repositories/aiDigestRepo', () => ({
  createAiDigestConfig: vi.fn(),
}));

describe('aiDigestLifecycleService', () => {
  it('creates ai digest feed + config in a transaction', async () => {
    const pool = {
      connect: connectMock.mockResolvedValue({
        query: queryMock,
        release: releaseMock,
      }),
    };

    const { createAiDigestWithCategoryResolution } = await import('./aiDigestLifecycleService');
    await expect(
      createAiDigestWithCategoryResolution(pool as never, {
        title: 'My Digest',
        prompt: '解读这些文章',
        intervalMinutes: 60,
        selectedFeedIds: [],
        selectedCategoryIds: [],
        categoryName: 'Tech',
      }),
    ).resolves.toBeTruthy();
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test:unit src/server/services/aiDigestLifecycleService.test.ts`

Expected: FAIL（module 不存在 / mock 不满足）

- [ ] **Step 3: 实现 `createAiDigestWithCategoryResolution`（事务 + 分类解析 + feed/config 写入）**

建议实现（关键点：分类名 normalize 规则与 `feedCategoryLifecycleService.ts` 对齐；`未分类` -> null）：

```ts
import crypto from 'node:crypto';
import type { Pool } from 'pg';
import {
  createCategory,
  findCategoryByNormalizedName,
  getNextCategoryPosition,
} from '../repositories/categoriesRepo';
import { createAiDigestFeed } from '../repositories/feedsRepo';
import { createAiDigestConfig } from '../repositories/aiDigestRepo';

type CategoryResolutionInput = {
  categoryId?: string | null;
  categoryName?: string | null;
};

function normalizeCategoryName(name: string | null | undefined): string | null {
  const normalized = name?.trim() ?? '';
  if (!normalized || normalized === '未分类') return null;
  return normalized;
}

async function resolveCategoryId(
  client: { query: Pool['query'] },
  input: CategoryResolutionInput,
): Promise<string | null> {
  if (typeof input.categoryId !== 'undefined') {
    return input.categoryId ?? null;
  }

  const normalizedName = normalizeCategoryName(input.categoryName);
  if (!normalizedName) return null;

  const existing = await findCategoryByNormalizedName(client as never, normalizedName);
  if (existing) return existing.id;

  const position = await getNextCategoryPosition(client as never);
  const created = await createCategory(client as never, { name: normalizedName, position });
  return created.id;
}

export async function createAiDigestWithCategoryResolution(
  pool: Pool,
  input: {
    title: string;
    prompt: string;
    intervalMinutes: number;
    selectedFeedIds: string[];
    selectedCategoryIds: string[];
    categoryId?: string | null;
    categoryName?: string | null;
  },
) {
  const client = await pool.connect();
  try {
    await client.query('begin');

    const feedId = crypto.randomUUID();
    const categoryId = await resolveCategoryId(client as never, input);

    const createdFeed = await createAiDigestFeed(client as never, {
      id: feedId,
      title: input.title,
      categoryId,
    });

    await createAiDigestConfig(client as never, {
      feedId,
      prompt: input.prompt,
      intervalMinutes: input.intervalMinutes,
      topN: 10,
      selectedFeedIds: input.selectedFeedIds,
      selectedCategoryIds: input.selectedCategoryIds,
      lastWindowEndAt: new Date().toISOString(),
    });

    await client.query('commit');
    return createdFeed;
  } catch (error) {
    await client.query('rollback');
    throw error;
  } finally {
    client.release();
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test:unit src/server/services/aiDigestLifecycleService.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/server/services/aiDigestLifecycleService.ts src/server/services/aiDigestLifecycleService.test.ts
git commit -m "feat(ai-digest): 添加解读源创建编排服务" \
  -m "- 复用分类解析规则并在事务中写入 feed 与 config" \
  -m "- 初始化 last_window_end_at 为 now() 以避免回看历史"
```

### Task 7: API - `POST /api/ai-digests` 创建 AI解读源

**Files:**

- Create: `src/app/api/ai-digests/route.ts`
- Test: `src/app/api/ai-digests/routes.test.ts`（本文件后续也覆盖 generate endpoint）

- [ ] **Step 1: 先写 route 测试（应当失败）**

```ts
import { beforeEach, describe, expect, it, vi } from 'vitest';

const pool = { connect: vi.fn() };
const createAiDigestWithCategoryResolutionMock = vi.fn();

vi.mock('../../../server/db/pool', () => ({ getPool: () => pool }));
vi.mock('../../../../server/db/pool', () => ({ getPool: () => pool }));

vi.mock('../../../server/services/aiDigestLifecycleService', () => ({
  createAiDigestWithCategoryResolution: (...args: unknown[]) =>
    createAiDigestWithCategoryResolutionMock(...args),
}));
vi.mock('../../../../server/services/aiDigestLifecycleService', () => ({
  createAiDigestWithCategoryResolution: (...args: unknown[]) =>
    createAiDigestWithCategoryResolutionMock(...args),
}));

describe('/api/ai-digests', () => {
  beforeEach(() => {
    createAiDigestWithCategoryResolutionMock.mockReset();
  });

  it('POST creates ai_digest feed and returns unreadCount=0', async () => {
    createAiDigestWithCategoryResolutionMock.mockResolvedValue({
      id: '00000000-0000-0000-0000-000000000000',
      kind: 'ai_digest',
      title: 'My Digest',
      url: 'http://localhost/__feedfuse_ai_digest__/00000000-0000-0000-0000-000000000000',
      siteUrl: null,
      iconUrl: null,
      enabled: true,
      fullTextOnOpenEnabled: false,
      aiSummaryOnOpenEnabled: false,
      aiSummaryOnFetchEnabled: false,
      bodyTranslateOnFetchEnabled: false,
      bodyTranslateOnOpenEnabled: false,
      titleTranslateEnabled: false,
      bodyTranslateEnabled: false,
      articleListDisplayMode: 'card',
      categoryId: null,
      fetchIntervalMinutes: 30,
      lastFetchStatus: null,
      lastFetchError: null,
    });

    const mod = await import('./route');
    const res = await mod.POST(
      new Request('http://localhost/api/ai-digests', {
        method: 'POST',
        headers: { 'content-type': 'application/json' },
        body: JSON.stringify({
          title: 'My Digest',
          prompt: '解读这些文章',
          intervalMinutes: 60,
          selectedFeedIds: [],
          selectedCategoryIds: [],
          categoryName: 'Tech',
        }),
      }),
    );
    const json = await res.json();

    expect(json.ok).toBe(true);
    expect(json.data.unreadCount).toBe(0);
    expect(json.data.kind).toBe('ai_digest');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test:unit src/app/api/ai-digests/routes.test.ts -t "POST creates ai_digest feed"`

Expected: FAIL（route 不存在）

- [ ] **Step 3: 实现 route（Zod 校验 + 调用 lifecycle service）**

`src/app/api/ai-digests/route.ts` 建议完整实现：

```ts
import { z } from 'zod';
import { getPool } from '../../../server/db/pool';
import { ok, fail } from '../../../server/http/apiResponse';
import { ValidationError } from '../../../server/http/errors';
import { createAiDigestWithCategoryResolution } from '../../../server/services/aiDigestLifecycleService';

export const runtime = 'nodejs';
export const dynamic = 'force-dynamic';

const categoryInputShape = {
  categoryId: z.string().uuid().nullable().optional(),
  categoryName: z.string().trim().min(1).nullable().optional(),
};

const INTERVAL_OPTIONS_MINUTES = [60, 120, 240, 480, 1440] as const;

const bodySchema = z
  .object({
    title: z.string().trim().min(1),
    prompt: z.string().trim().min(1),
    intervalMinutes: z.number().int(),
    selectedFeedIds: z.array(z.string().uuid()).default([]),
    selectedCategoryIds: z.array(z.string().uuid()).default([]),
    ...categoryInputShape,
  })
  .refine((value) => !(value.categoryId && value.categoryName), {
    path: ['categoryName'],
    message: 'categoryId and categoryName are mutually exclusive',
  })
  .refine((value) => INTERVAL_OPTIONS_MINUTES.includes(value.intervalMinutes as never), {
    path: ['intervalMinutes'],
    message: 'intervalMinutes is not in allowed options',
  })
  .refine((value) => value.selectedFeedIds.length > 0 || value.selectedCategoryIds.length > 0, {
    path: ['selectedFeedIds'],
    message: 'at least one source is required',
  });

function zodIssuesToFields(error: z.ZodError): Record<string, string> {
  const fields: Record<string, string> = {};
  for (const issue of error.issues) {
    const key = issue.path.join('.') || 'body';
    if (!fields[key]) fields[key] = issue.message;
  }
  return fields;
}

export async function POST(request: Request) {
  try {
    const json = await request.json().catch(() => null);
    const parsed = bodySchema.safeParse(json);
    if (!parsed.success) {
      return fail(new ValidationError('Invalid request body', zodIssuesToFields(parsed.error)));
    }

    const pool = getPool();
    const created = await createAiDigestWithCategoryResolution(pool, parsed.data);
    return ok({ ...created, unreadCount: 0 });
  } catch (err) {
    return fail(err);
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test:unit src/app/api/ai-digests/routes.test.ts -t "POST creates ai_digest feed"`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/app/api/ai-digests/route.ts src/app/api/ai-digests/routes.test.ts
git commit -m "feat(api): 添加 AI解读源创建接口" \
  -m "- 新增 POST /api/ai-digests 并复用分类解析规则" \
  -m "- 校验 intervalMinutes 固定选项与来源必填"
```

### Task 8: API - `POST /api/ai-digests/:feedId/generate`（手动立即生成）

**Files:**

- Create: `src/app/api/ai-digests/[feedId]/generate/route.ts`
- Modify: `src/app/api/ai-digests/routes.test.ts`

- [ ] **Step 1: 先写失败测试：缺失 API key 时返回 reason 且不创建 run/不入队**

```ts
import { beforeEach, describe, expect, it, vi } from 'vitest';
import { getQueueSendOptions } from '../../../server/queue/contracts';
import { JOB_AI_DIGEST_GENERATE } from '../../../server/queue/jobs';

const pool = { query: vi.fn() };
const getAiApiKeyMock = vi.fn();
const getAiDigestConfigByFeedIdMock = vi.fn();
const createAiDigestRunMock = vi.fn();
const getAiDigestRunByFeedIdAndWindowStartAtMock = vi.fn();
const updateAiDigestRunMock = vi.fn();
const enqueueWithResultMock = vi.fn();

vi.mock('../../../server/db/pool', () => ({ getPool: () => pool }));
vi.mock('../../../../server/db/pool', () => ({ getPool: () => pool }));

vi.mock('../../../server/repositories/settingsRepo', () => ({
  getAiApiKey: (...args: unknown[]) => getAiApiKeyMock(...args),
}));
vi.mock('../../../../server/repositories/settingsRepo', () => ({
  getAiApiKey: (...args: unknown[]) => getAiApiKeyMock(...args),
}));

vi.mock('../../../server/repositories/aiDigestRepo', () => ({
  getAiDigestConfigByFeedId: (...args: unknown[]) => getAiDigestConfigByFeedIdMock(...args),
  createAiDigestRun: (...args: unknown[]) => createAiDigestRunMock(...args),
  getAiDigestRunByFeedIdAndWindowStartAt: (...args: unknown[]) =>
    getAiDigestRunByFeedIdAndWindowStartAtMock(...args),
  updateAiDigestRun: (...args: unknown[]) => updateAiDigestRunMock(...args),
}));
vi.mock('../../../../server/repositories/aiDigestRepo', () => ({
  getAiDigestConfigByFeedId: (...args: unknown[]) => getAiDigestConfigByFeedIdMock(...args),
  createAiDigestRun: (...args: unknown[]) => createAiDigestRunMock(...args),
  getAiDigestRunByFeedIdAndWindowStartAt: (...args: unknown[]) =>
    getAiDigestRunByFeedIdAndWindowStartAtMock(...args),
  updateAiDigestRun: (...args: unknown[]) => updateAiDigestRunMock(...args),
}));

vi.mock('../../../server/queue/queue', () => ({
  enqueueWithResult: (...args: unknown[]) => enqueueWithResultMock(...args),
}));
vi.mock('../../../../server/queue/queue', () => ({
  enqueueWithResult: (...args: unknown[]) => enqueueWithResultMock(...args),
}));

describe('/api/ai-digests/:feedId/generate', () => {
  beforeEach(() => {
    getAiApiKeyMock.mockReset();
    getAiDigestConfigByFeedIdMock.mockReset();
    createAiDigestRunMock.mockReset();
    getAiDigestRunByFeedIdAndWindowStartAtMock.mockReset();
    updateAiDigestRunMock.mockReset();
    enqueueWithResultMock.mockReset();
  });

  it('returns missing_api_key and does not create runs', async () => {
    getAiApiKeyMock.mockResolvedValue('');

    const mod = await import('./[feedId]/generate/route');
    const res = await mod.POST(
      new Request('http://localhost/api/ai-digests/x/generate', { method: 'POST' }),
      { params: Promise.resolve({ feedId: '00000000-0000-0000-0000-000000000000' }) },
    );
    const json = await res.json();

    expect(json.ok).toBe(true);
    expect(json.data.enqueued).toBe(false);
    expect(json.data.reason).toBe('missing_api_key');
    expect(createAiDigestRunMock).not.toHaveBeenCalled();
    expect(enqueueWithResultMock).not.toHaveBeenCalled();
  });

  it('enqueues ai.digest_generate when config exists and not already running', async () => {
    getAiApiKeyMock.mockResolvedValue('sk-test');
    getAiDigestConfigByFeedIdMock.mockResolvedValue({
      feedId: '00000000-0000-0000-0000-000000000000',
      lastWindowEndAt: '2026-03-14T00:00:00.000Z',
    });
    getAiDigestRunByFeedIdAndWindowStartAtMock.mockResolvedValue(null);
    createAiDigestRunMock.mockResolvedValue({
      id: '11111111-1111-1111-1111-111111111111',
    });
    enqueueWithResultMock.mockResolvedValue({ status: 'enqueued', jobId: 'job-1' });

    const mod = await import('./[feedId]/generate/route');
    const res = await mod.POST(
      new Request('http://localhost/api/ai-digests/x/generate', { method: 'POST' }),
      { params: Promise.resolve({ feedId: '00000000-0000-0000-0000-000000000000' }) },
    );
    const json = await res.json();

    expect(json.ok).toBe(true);
    expect(json.data.enqueued).toBe(true);
    expect(enqueueWithResultMock).toHaveBeenCalledWith(
      JOB_AI_DIGEST_GENERATE,
      { runId: '11111111-1111-1111-1111-111111111111' },
      getQueueSendOptions(JOB_AI_DIGEST_GENERATE, { runId: '11111111-1111-1111-1111-111111111111' }),
    );
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test:unit src/app/api/ai-digests/routes.test.ts -t "returns missing_api_key"`

Expected: FAIL（route 不存在 / JOB 常量未定义）

- [ ] **Step 3: 实现 generate route（模式参考 `src/app/api/articles/[id]/ai-summary/route.ts`）**

`src/app/api/ai-digests/[feedId]/generate/route.ts` 建议实现：

```ts
import { z } from 'zod';
import { getPool } from '../../../../../server/db/pool';
import { ok, fail } from '../../../../../server/http/apiResponse';
import { NotFoundError, ValidationError } from '../../../../../server/http/errors';
import { getAiApiKey } from '../../../../../server/repositories/settingsRepo';
import {
  createAiDigestRun,
  getAiDigestConfigByFeedId,
  getAiDigestRunByFeedIdAndWindowStartAt,
  updateAiDigestRun,
} from '../../../../../server/repositories/aiDigestRepo';
import { getQueueSendOptions } from '../../../../../server/queue/contracts';
import { enqueueWithResult } from '../../../../../server/queue/queue';
import { JOB_AI_DIGEST_GENERATE } from '../../../../../server/queue/jobs';

export const runtime = 'nodejs';
export const dynamic = 'force-dynamic';

const paramsSchema = z.object({
  feedId: z.string().uuid(),
});

function zodIssuesToFields(error: z.ZodError): Record<string, string> {
  const fields: Record<string, string> = {};
  for (const issue of error.issues) {
    const key = issue.path.join('.') || 'params';
    if (!fields[key]) fields[key] = issue.message;
  }
  return fields;
}

export async function POST(
  _request: Request,
  context: { params: Promise<{ feedId: string }> },
) {
  try {
    const params = await context.params;
    const parsedParams = paramsSchema.safeParse(params);
    if (!parsedParams.success) {
      return fail(new ValidationError('Invalid route params', zodIssuesToFields(parsedParams.error)));
    }

    const pool = getPool();
    const aiApiKey = await getAiApiKey(pool);
    if (!aiApiKey.trim()) {
      return ok({ enqueued: false, reason: 'missing_api_key' });
    }

    const feedId = parsedParams.data.feedId;
    const config = await getAiDigestConfigByFeedId(pool, feedId);
    if (!config) return fail(new NotFoundError('AI digest config not found'));

    const windowStartAt = config.lastWindowEndAt;
    const windowEndAt = new Date().toISOString();

    const existing = await getAiDigestRunByFeedIdAndWindowStartAt(pool, { feedId, windowStartAt });
    if (existing && (existing.status === 'queued' || existing.status === 'running')) {
      return ok({ enqueued: false, reason: 'already_running', runId: existing.id });
    }

    const created =
      (existing && existing.status === 'failed') ? existing : await createAiDigestRun(pool, {
        feedId,
        windowStartAt,
        windowEndAt,
        status: 'queued',
      });

    if (!created) {
      const again = await getAiDigestRunByFeedIdAndWindowStartAt(pool, { feedId, windowStartAt });
      if (again && (again.status === 'queued' || again.status === 'running')) {
        return ok({ enqueued: false, reason: 'already_running', runId: again.id });
      }
      if (!again) throw new Error('Failed to create or load ai digest run');
      // allow manual retry for failed
      if (again.status !== 'failed') {
        return ok({ enqueued: false, reason: 'already_running', runId: again.id });
      }
    }

    const runId = created ? created.id : (await getAiDigestRunByFeedIdAndWindowStartAt(pool, { feedId, windowStartAt }))!.id;
    const payload = { runId };
    const enqueueResult = await enqueueWithResult(
      JOB_AI_DIGEST_GENERATE,
      payload,
      getQueueSendOptions(JOB_AI_DIGEST_GENERATE, { runId }),
    );
    if (enqueueResult.status !== 'enqueued') {
      return ok({ enqueued: false, reason: 'already_running', runId });
    }

    await updateAiDigestRun(pool, runId, { jobId: enqueueResult.jobId, status: 'queued', errorCode: null, errorMessage: null });
    return ok({ enqueued: true, jobId: enqueueResult.jobId, runId });
  } catch (err) {
    return fail(err);
  }
}
```

（上面实现为了避免重复：`createAiDigestRun()` 用唯一约束 + `on conflict do nothing`，并在 `enqueueWithResult` 侧用 singletonKey 做去重。）

- [ ] **Step 4: 更新/实现 queue jobs 与 contracts（让测试能 import `JOB_AI_DIGEST_GENERATE`）**

本步骤会在 Chunk 4 做完整实现；为了让本 Task 的 route 测试先跑通，可以先在 `src/server/queue/jobs.ts` 增加常量并在 `src/server/queue/jobs.test.ts` 里补断言（见 Chunk 4 Task 14）。

- [ ] **Step 5: 运行测试确认通过**

Run: `pnpm test:unit src/app/api/ai-digests/routes.test.ts -t "returns missing_api_key"`

Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/app/api/ai-digests/[feedId]/generate/route.ts src/app/api/ai-digests/routes.test.ts
git commit -m "feat(api): 添加 AI解读源立即生成接口" \
  -m "- 新增 POST /api/ai-digests/:feedId/generate 创建 run 并入队" \
  -m "- 缺失 AI API key 时返回 reason 并避免后台堆积"
```

---

## Chunk 3: Client/Store/UI（创建入口 + 立即生成按钮）

### Task 9: 客户端类型与 API client（`Feed.kind` + `createAiDigest`/`generateAiDigest`）

**Files:**

- Modify: `src/types/index.ts`
- Modify: `src/lib/apiClient.ts`
- Modify: `src/lib/apiClient.test.ts`

- [ ] **Step 1: 先写失败测试：`mapFeedDto` 必须透传 `kind`**

在 `src/lib/apiClient.test.ts` 添加一个最小断言（靠 import + 调用）：

```ts
import { describe, expect, it } from 'vitest';

describe('mapFeedDto', () => {
  it('maps kind', async () => {
    const { mapFeedDto } = await import('./apiClient');
    const out = mapFeedDto(
      {
        id: 'f1',
        kind: 'ai_digest',
        title: 'Digest',
        url: 'http://localhost/__feedfuse_ai_digest__/f1',
        siteUrl: null,
        iconUrl: null,
        enabled: true,
        fullTextOnOpenEnabled: false,
        aiSummaryOnOpenEnabled: false,
        aiSummaryOnFetchEnabled: false,
        bodyTranslateOnFetchEnabled: false,
        bodyTranslateOnOpenEnabled: false,
        titleTranslateEnabled: false,
        bodyTranslateEnabled: false,
        articleListDisplayMode: 'card',
        categoryId: null,
        fetchIntervalMinutes: 30,
        lastFetchStatus: null,
        lastFetchError: null,
        unreadCount: 0,
      },
      [],
    );
    expect(out.kind).toBe('ai_digest');
  });
});
```

- [ ] **Step 2: 实现类型透传**

1) `src/types/index.ts`：

```ts
export type FeedKind = 'rss' | 'ai_digest';

export interface Feed {
  // ...
  kind: FeedKind;
}
```

2) `src/lib/apiClient.ts`：

- `ReaderSnapshotDto['feeds']` item 与 `FeedRowDto` 增加 `kind`
- `mapFeedDto()` 增加 `kind: dto.kind`
- 增加两条 client helper：

```ts
export async function createAiDigest(input: {
  title: string;
  prompt: string;
  intervalMinutes: number;
  selectedFeedIds: string[];
  selectedCategoryIds: string[];
  categoryId?: string | null;
  categoryName?: string | null;
}): Promise<ReaderSnapshotDto['feeds'][number] & { unreadCount: number }> {
  return requestApi('/api/ai-digests', {
    method: 'POST',
    headers: { 'content-type': 'application/json' },
    body: JSON.stringify(input),
  });
}

export async function generateAiDigest(feedId: string): Promise<{ enqueued: boolean; jobId?: string; reason?: string; runId?: string }> {
  return requestApi(`/api/ai-digests/${encodeURIComponent(feedId)}/generate`, {
    method: 'POST',
  });
}
```

- [ ] **Step 3: 运行测试确认通过**

Run: `pnpm test:unit src/lib/apiClient.test.ts -t "maps kind"`

Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add src/types/index.ts src/lib/apiClient.ts src/lib/apiClient.test.ts
git commit -m "feat(client): 支持 feed.kind 与 AI解读接口" \
  -m "- 为 Feed/DTO 透传 kind 以支持 UI 分支" \
  -m "- 添加 createAiDigest 与 generateAiDigest client helper"
```

### Task 10: Store - 新增 `addAiDigest()`（创建后只刷新 snapshot）

**Files:**

- Modify: `src/store/appStore.ts`

- [ ] **Step 1: 先写失败的最小回归（通过组件测试驱动）**

这里建议不要为 store 单独新建测试文件：直接在后续 `AddAiDigestDialog.test.tsx` 里 mock `useAppStore` 并断言调用 `addAiDigest()`，更贴近真实 UI 交互。

- [ ] **Step 2: 实现 `addAiDigest()`**

在 `src/store/appStore.ts`：

- `AppState` 增加方法签名：

```ts
  addAiDigest: (payload: {
    title: string;
    prompt: string;
    intervalMinutes: number;
    selectedFeedIds: string[];
    selectedCategoryIds: string[];
    categoryId?: string | null;
    categoryName?: string | null;
  }) => Promise<void>;
```

- create store 实现：

```ts
  addAiDigest: async (payload) => {
    const created = await createAiDigest(payload);
    const categories = get().categories;
    const mapped = mapFeedDto(created, categories);

    set((state) => ({
      feeds: state.feeds.some((item) => item.id === mapped.id) ? state.feeds : [...state.feeds, mapped],
      selectedView: mapped.id,
      selectedArticleId: null,
    }));

    await get().loadSnapshot({ view: mapped.id });
  },
```

注意：这里 **禁止** 调用 `refreshFeed()` 或任何 RSS 抓取 enqueue（符合 spec）。

- [ ] **Step 3: Commit**

```bash
git add src/store/appStore.ts
git commit -m "feat(store): 添加 AI解读源创建动作" \
  -m "- 新增 addAiDigest 并在创建后仅 loadSnapshot" \
  -m "- 避免复用 addFeed 的 refreshFeed 语义"
```

### Task 11: FeedList `+` 按钮改为菜单（添加 RSS / 添加 AI解读）

**Files:**

- Modify: `src/features/feeds/FeedList.tsx`
- Modify: `src/features/feeds/FeedList.test.tsx`

- [ ] **Step 1: 更新/新增测试：顶部按钮可打开菜单并出现两项中文文案**

在 `src/features/feeds/FeedList.test.tsx` 找到当前对 `aria-label="添加 RSS 源"` 的断言，调整为：

- `+` 按钮 `aria-label="添加订阅"`（或保留旧 label 但菜单内提供两项）
- 点击后应出现：
  - `添加 RSS 源`
  - `添加 AI解读`

（测试风格参考现有 `ContextMenu`/Dialog 交互测试用法）

- [ ] **Step 2: 实现 UI：使用 `Popover` 实现轻量菜单**

建议实现（避免引入新组件库）：

- 引入 `Popover`, `PopoverTrigger`, `PopoverContent`
- 用 `PopoverTrigger` 包裹原 `+` 按钮
- `PopoverContent` 内放两个 `Button` 或 `<button>`：
  - 点击 `添加 RSS 源`：`setAddFeedOpen(true)`
  - 点击 `添加 AI解读`：`setAddAiDigestOpen(true)`

- [ ] **Step 3: Commit**

```bash
git add src/features/feeds/FeedList.tsx src/features/feeds/FeedList.test.tsx
git commit -m "feat(ui): 添加 AI解读入口并升级添加菜单" \
  -m "- 将左栏 + 按钮改为 Popover 菜单" \
  -m "- 提供 添加 RSS 源 / 添加 AI解读 两个入口"
```

### Task 12: 新增 `AddAiDigestDialog`（单页表单：标题/提示词/来源/间隔/分类）

**Files:**

- Create: `src/features/feeds/AddAiDigestDialog.tsx`
- Create: `src/features/feeds/AiDigestDialog.tsx`
- Create: `src/features/feeds/AiDigestDialogForm.tsx`
- Create: `src/features/feeds/useAiDigestDialogForm.ts`
- Test: `src/features/feeds/AddAiDigestDialog.test.tsx`
- Modify: `src/features/feeds/FeedList.tsx`（引入 dialog 并传参）

- [ ] **Step 1: 先写失败测试：必填校验 + 成功提交会调用 `useAppStore().addAiDigest`**

```tsx
import { describe, expect, it, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';

vi.mock('../../store/appStore', () => ({
  useAppStore: (selector: unknown) => {
    const state = {
      categories: [{ id: 'c1', name: 'Tech', expanded: true }],
      feeds: [{ id: 'f1', kind: 'rss', title: 'RSS 1', unreadCount: 0, url: 'https://x', enabled: true, fullTextOnOpenEnabled: false, aiSummaryOnOpenEnabled: false, aiSummaryOnFetchEnabled: false, bodyTranslateOnFetchEnabled: false, bodyTranslateOnOpenEnabled: false, titleTranslateEnabled: false, bodyTranslateEnabled: false, articleListDisplayMode: 'card', fetchStatus: null, fetchError: null }],
      addAiDigest: vi.fn(),
    };
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    return typeof selector === 'function' ? (selector as any)(state) : state;
  },
}));

describe('AddAiDigestDialog', () => {
  it('requires title, prompt and at least one source', async () => {
    const { default: AddAiDigestDialog } = await import('./AddAiDigestDialog');
    render(
      <AddAiDigestDialog
        open
        onOpenChange={() => {}}
        categories={[{ id: 'c1', name: 'Tech', expanded: true }]}
        feeds={[
          {
            id: 'f1',
            kind: 'rss',
            title: 'RSS 1',
            url: 'https://example.com/rss.xml',
            siteUrl: null,
            unreadCount: 0,
            enabled: true,
            fullTextOnOpenEnabled: false,
            aiSummaryOnOpenEnabled: false,
            aiSummaryOnFetchEnabled: false,
            bodyTranslateOnFetchEnabled: false,
            bodyTranslateOnOpenEnabled: false,
            titleTranslateEnabled: false,
            bodyTranslateEnabled: false,
            articleListDisplayMode: 'card',
            fetchStatus: null,
            fetchError: null,
          },
        ]}
      />,
    );

    fireEvent.click(screen.getByRole('button', { name: '创建 AI解读源' }));
    expect(screen.getByText('标题为必填项')).toBeInTheDocument();
    expect(screen.getByText('AI解读提示词为必填项')).toBeInTheDocument();
    expect(screen.getByText('请至少选择一个来源')).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 实现 dialog + form**

核心实现建议：

- `AiDigestDialogForm.tsx` 负责 UI：
  - `标题`（Input）
  - `AI解读`（Textarea）
  - `来源`：两组多选（RSS feeds / 分类）：
    - RSS feeds 列表只显示 `feeds.filter((f) => f.kind === 'rss')`
    - 分类列表使用 `categories`（排除 `id==='cat-uncategorized'` 也可以）
  - `重复时间`：Select，固定 options（与服务端一致：`[60, 120, 240, 480, 1440]`）
  - `分类`：复用 `CreatableCategoryField`
  - 说明文案：强调“只会解读窗口内更新，最终只纳入 Top 10 篇”
- `useAiDigestDialogForm.ts` 负责：
  - state：`title/prompt/intervalMinutes/categoryInput/selectedFeedIds/selectedCategoryIds`
  - submit：调用 `useAppStore.getState().addAiDigest(...)`
  - 成功：toast `已创建 AI解读源`，并 `onOpenChange(false)`

- [ ] **Step 3: 运行测试确认通过**

Run: `pnpm test:unit src/features/feeds/AddAiDigestDialog.test.tsx`

Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add src/features/feeds/AddAiDigestDialog.tsx src/features/feeds/AiDigestDialog.tsx src/features/feeds/AiDigestDialogForm.tsx src/features/feeds/useAiDigestDialogForm.ts src/features/feeds/AddAiDigestDialog.test.tsx src/features/feeds/FeedList.tsx
git commit -m "feat(ui): 添加 AI解读源创建表单" \
  -m "- 新增单页表单支持标题/提示词/来源多选/间隔/分类" \
  -m "- 创建成功后仅刷新 snapshot，不触发 RSS 抓取"
```

### Task 13: FeedList 右键菜单对 `ai_digest` 隐藏 RSS 专属项

**Files:**

- Modify: `src/features/feeds/FeedList.tsx`
- Modify: `src/features/feeds/FeedList.test.tsx`

- [ ] **Step 1: 更新测试：`ai_digest` feed 不应出现“全文抓取配置/AI摘要配置/翻译配置/配置关键词过滤/编辑”**

实现策略：在测试里构造一个 `kind: 'ai_digest'` 的 feed，并断言 context menu 不含这些项，但保留 `移动到分类/启用/停用/删除`。

- [ ] **Step 2: 实现：用 `if (feed.kind === 'rss')` 包裹 RSS-only menu items**

最小改动：

- `编辑`、`全文抓取配置`、`AI摘要配置`、`翻译配置`、`配置关键词过滤` 仅对 `rss` 渲染
- `移动到分类`、`启用/停用`、`删除` 对两种 kind 都保留

- [ ] **Step 3: Commit**

```bash
git add src/features/feeds/FeedList.tsx src/features/feeds/FeedList.test.tsx
git commit -m "fix(ui): AI解读源隐藏 RSS 专属菜单项" \
  -m "- 对 ai_digest 禁用编辑与全文/摘要/翻译/关键词过滤配置" \
  -m "- 保留分类移动、启停与删除操作"
```

### Task 14: ArticleList 工具栏：`ai_digest` feed view 替换为“立即生成”

**Files:**

- Modify: `src/features/articles/ArticleList.tsx`
- Modify: `src/features/articles/ArticleList.test.tsx`

- [ ] **Step 1: 先写失败测试：当 selectedFeed.kind==='ai_digest' 时出现按钮 label “立即生成”**

思路：在现有 `ArticleList.test.tsx`（已经大量 mock store/snapshot）里新增一个 case：

- store feeds 包含一个 `kind:'ai_digest'` 的 feed
- selectedView = 该 feed.id
- 断言工具栏按钮 `aria-label` 为 `立即生成`，且不存在 `刷新订阅源`

- [ ] **Step 2: 实现 UI 分支**

在 `src/features/articles/ArticleList.tsx`：

- `selectedFeed` 已可用，新增：

```ts
  const isAiDigestView = Boolean(selectedFeed && selectedFeed.kind === 'ai_digest');
  const primaryActionLabel = isAggregateView
    ? '刷新全部订阅源'
    : isAiDigestView
      ? '立即生成'
      : '刷新订阅源';
```

- 当 `isAiDigestView` 时：
  - 点击调用 `generateAiDigest(selectedFeed.id)`
  - reason 处理（至少支持）：
    - `missing_api_key`：toast.error `请先在设置中配置 AI API Key`
    - `already_running`：toast.success/info `已在生成中`
  - enqueue 成功后：toast.success `已开始生成 AI 解读`
  - 轮询 snapshot：保存触发前的 digest article id 集合，`loadSnapshot({ view })` 直到出现新 id 或超时（例如 30s）
  - 若超时且无新文章：toast `本次窗口无更新，未生成解读（可稍后重试）`

- [ ] **Step 3: 运行测试确认通过**

Run: `pnpm test:unit src/features/articles/ArticleList.test.tsx -t "立即生成"`

Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add src/features/articles/ArticleList.tsx src/features/articles/ArticleList.test.tsx
git commit -m "feat(reader): AI解读源支持立即生成" \
  -m "- 在 ai_digest view 用 立即生成 替换 刷新订阅源" \
  -m "- 触发后轮询 snapshot 以展示新生成的解读文章"
```

---

## Chunk 4: Queue/Worker/AI（tick + generate + rerank + compose）

### Task 15: Queue job 常量与 contract（`ai.digest_tick` / `ai.digest_generate`）

**Files:**

- Modify: `src/server/queue/jobs.ts`
- Modify: `src/server/queue/jobs.test.ts`
- Modify: `src/server/queue/contracts.ts`

- [ ] **Step 1: 添加 job 常量并补测试**

`src/server/queue/jobs.ts`：

```ts
export const JOB_AI_DIGEST_TICK = 'ai.digest_tick';
export const JOB_AI_DIGEST_GENERATE = 'ai.digest_generate';
```

`src/server/queue/jobs.test.ts` 增加断言：

```ts
expect(JOB_AI_DIGEST_TICK).toBe('ai.digest_tick');
expect(JOB_AI_DIGEST_GENERATE).toBe('ai.digest_generate');
```

- [ ] **Step 2: 在 `contracts.ts` 注册 queue contract**

扩展 `SendContext`：

```ts
type SendContext = {
  articleId?: string;
  feedId?: string;
  runId?: string;
  force?: boolean;
};
```

新增 contract（建议）：

```ts
  'ai.digest_tick': {
    queue: { warningQueueSize: 5 },
    worker: { localConcurrency: 1, batchSize: 1 },
    send: () => ({ singletonKey: 'ai.digest_tick', singletonSeconds: 55 }),
  },
  'ai.digest_generate': {
    queue: {
      retryLimit: 3,
      retryDelay: 30,
      retryBackoff: true,
      retryDelayMax: 600,
      heartbeatSeconds: 60,
      expireInSeconds: 1800,
      warningQueueSize: 50,
    },
    worker: { localConcurrency: 1, batchSize: 1 },
    send: (ctx) =>
      ctx.runId ? { singletonKey: ctx.runId, singletonSeconds: 3600 } : {},
  },
```

- [ ] **Step 3: 运行测试**

Run: `pnpm test:unit src/server/queue/jobs.test.ts`

Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add src/server/queue/jobs.ts src/server/queue/jobs.test.ts src/server/queue/contracts.ts
git commit -m "feat(queue): 添加 AI解读 tick 与生成任务" \
  -m "- 新增 ai.digest_tick 与 ai.digest_generate job 常量" \
  -m "- 配置 singletonKey 与重试退避以避免重复生成"
```

### Task 16: AI helper - `aiDigestRerank`（增量 shortlist rerank）

**Files:**

- Create: `src/server/ai/aiDigestRerank.ts`
- Test: `src/server/ai/aiDigestRerank.test.ts`

- [ ] **Step 1: 先写失败测试：会调用 `/chat/completions` 且只输出 JSON array**

测试写法可参考 `src/server/ai/summarizeText.test.ts`（stub global `fetch`）。

- [ ] **Step 2: 实现（建议完整实现）**

关键契约：

- 输入：`prompt` + `shortlist[]` + `batch[]`（每项含 `id/feedTitle/title/summary/link/fetchedAt`）
- 输出：长度 <= `topN` 的 `id[]`（必须是输入集合子集）
- 解析：复用 `unwrapCodeFence` 风格（参考 `bilingualHtmlTranslator.ts`）

（实现略；复制 `bilingualHtmlTranslator.ts` 的 `unwrapCodeFence`/JSON parse 逻辑即可。）

- [ ] **Step 3: Commit**

```bash
git add src/server/ai/aiDigestRerank.ts src/server/ai/aiDigestRerank.test.ts
git commit -m "feat(ai): 添加 AI解读候选筛选能力" \
  -m "- 实现增量 shortlist rerank 以控制上下文大小" \
  -m "- 失败时允许上层降级为按 fetched_at 取最新"
```

### Task 17: AI helper - `aiDigestCompose`（Map-Reduce + 递归折叠，输出 `{ title, html }`）

**Files:**

- Create: `src/server/ai/aiDigestCompose.ts`
- Test: `src/server/ai/aiDigestCompose.test.ts`

- [ ] **Step 1: 先写失败测试：输出必须包含 `{ title, html }` 且可解析**

依旧 stub `fetch`，返回 JSON（可用 code fence 包裹）并验证解析。

- [ ] **Step 2: 实现**

关键点（按 spec）：

- 输入文章：每篇优先 `content_full_html`，否则 `title + summary`；先 HTML->纯文本->压缩空白->硬截断
- Map：3-5 篇/批，输出结构化要点（JSON）
- Reduce：把 Map 输出合并，要求模型输出 `{ title, html }`
- 递归折叠：当 Map 输出过长时先让模型压缩再 Reduce

（建议在 helper 内控制最大字符预算，例如 60k chars；超过则折叠）

- [ ] **Step 3: Commit**

```bash
git add src/server/ai/aiDigestCompose.ts src/server/ai/aiDigestCompose.test.ts
git commit -m "feat(ai): 添加 AI解读正文与标题生成能力" \
  -m "- 实现 map-reduce 生成并对输出做预算折叠" \
  -m "- 输出结构化 {title, html} 供 worker 落库"
```

### Task 18: Worker - `aiDigestTick`（每分钟扫描 due configs 并派发 generate job）

**Files:**

- Create: `src/worker/aiDigestTick.ts`
- Test: `src/worker/aiDigestTick.test.ts`
- Modify: `src/worker/index.ts`

- [ ] **Step 1: 先写失败测试：缺失 API key 直接 skip**

mock `getAiApiKey()` 返回空，并断言不调用 `boss.send`。

- [ ] **Step 2: 实现 tick**

建议把 tick 逻辑写成可注入 deps 的函数，避免在单测里 mock `getPool()`：

```ts
export async function runAiDigestTick(deps: {
  pool: { query: Function };
  boss: { send: Function };
  now?: Date;
}) { /* ... */ }
```

tick 行为：

- 若 `getAiApiKey()` 为空：直接 return（不创建 run、不入队）
- `listDueAiDigestConfigFeedIds(now)` 找 due feedIds
- 对每条 due：
  - load config -> `windowStartAt=config.lastWindowEndAt`、`windowEndAt=now`
  - 若已有 run 且 status in `queued|running|failed`：skip
  - 否则创建 run status `queued`
  - `boss.send(JOB_AI_DIGEST_GENERATE, { runId }, getQueueSendOptions(...))`
  - 若 jobId 存在：`updateAiDigestRun(runId, { jobId })`

- [ ] **Step 3: 在 `src/worker/index.ts` 注册 handler + schedule**

新增：

- `JOB_AI_DIGEST_TICK` handler：直接调用 `runAiDigestTick({ pool: getPool(), boss, now: new Date() })`
- `await boss.schedule(JOB_AI_DIGEST_TICK, '* * * * *')`
- 可选：启动时 `await boss.send(JOB_AI_DIGEST_TICK, {})`（与 refresh_all 一致）

- [ ] **Step 4: Commit**

```bash
git add src/worker/aiDigestTick.ts src/worker/aiDigestTick.test.ts src/worker/index.ts
git commit -m "feat(worker): 添加 AI解读 tick 调度" \
  -m "- 每分钟扫描 due configs 并创建 run 入队" \
  -m "- 缺失 AI API key 时跳过避免后台堆积"
```

### Task 19: Worker - `aiDigestGenerate`（run 执行：候选->筛选->解读->落库->推进游标）

**Files:**

- Create: `src/worker/aiDigestGenerate.ts`
- Test: `src/worker/aiDigestGenerate.test.ts`
- Modify: `src/worker/index.ts`

- [ ] **Step 1: 先写失败测试：候选为 0 时 status=skipped_no_updates 且推进 last_window_end_at**

mock repo：

- `getAiDigestRunById()` 返回 run（含 windowStart/End）
- `getAiDigestConfigByFeedId()` 返回 config
- `listAiDigestCandidateArticles()` 返回 `[]`
- 断言：
  - `updateAiDigestRun(..., { status: 'skipped_no_updates' })`
  - `updateAiDigestConfigLastWindowEndAt(feedId, windowEndAt)` 被调用

- [ ] **Step 2: 实现 generate（建议按 spec 顺序实现）**

建议实现为可注入 deps 的函数（便于单测）：

```ts
export async function runAiDigestGenerate(deps: {
  pool: { query: Function };
  runId: string;
  now?: Date;
  getAiApiKey: (pool: any) => Promise<string>;
}) { /* ... */ }
```

关键步骤（必须落地）：

1) 读取 run/config/feed
2) 解析 `targetFeedIds`：
   - `selected_feed_ids`：只保留 `feeds.kind='rss'` 的 ids
   - `selected_category_ids`：查分类下 `feeds.kind='rss'`
3) 候选查询：`fetched_at > windowStartAt and fetched_at <= windowEndAt`
4) 候选为 0：
   - run: `skipped_no_updates`
   - 推进 config `last_window_end_at = windowEndAt`
5) rerank Top 10：
   - 调用 `aiDigestRerank`（失败则降级 newest 10）
6) compose：
   - 调用 `aiDigestCompose` 输出 `{ title, html }`
   - 标题失败兜底 `feeds.title`
7) sanitize：`sanitizeContent(html)`，若结果为空视为失败
8) 落库：`insertArticleIgnoreDuplicate()`：
   - `feedId = ai_digest_feed_id`
   - `dedupeKey = 'ai_digest_run:' + runId`
   - `publishedAt = now().toISOString()`
   - `contentHtml = sanitized`
9) 完成：
   - run: `succeeded` + `articleId` + `selectedCount`
   - 推进 `last_window_end_at = windowEndAt`

错误处理（按 spec）：

- 捕获异常后写 run `failed`（error_code/error_message）
- 若 job 还有 retry：抛出错误交给 pg-boss retry（不推进 cursor）
- 若已经最终失败：推进 cursor 到 `windowEndAt` 避免卡死

> 注：pg-boss job object 通常包含 `retrycount`/`retrylimit`（字段名依版本可能为 snake），在 `src/worker/index.ts` handler 里把它提取成 `isFinalAttempt` 传给 `runAiDigestGenerate`，避免在 worker 内部强依赖 job 结构。

- [ ] **Step 3: 在 `src/worker/index.ts` 注册 JOB_AI_DIGEST_GENERATE handler**

解析 job data：

- `runId` 从 `job.data.runId` 读取（缺失则 throw）
- `jobId` 从 `job.id` 读取（用于 run.job_id 观测，可选）

- [ ] **Step 4: Commit**

```bash
git add src/worker/aiDigestGenerate.ts src/worker/aiDigestGenerate.test.ts src/worker/index.ts
git commit -m "feat(worker): 添加 AI解读生成执行器" \
  -m "- 实现候选筛选、解读生成、sanitize 与落库并推进窗口游标" \
  -m "- 通过 dedupe_key 与 singletonKey 保证幂等与重试安全"
```

---

## Final Verification Checklist

- [ ] 运行单测：`pnpm test:unit`
- [ ] 运行 lint：`pnpm lint`
- [ ] 本地跑迁移（需要 `DATABASE_URL`）：`node scripts/db/migrate.mjs`
- [ ] 启动服务与 worker（Docker 推荐）：
  - `docker compose up --build`
  - Web: `http://127.0.0.1:9559`
- [ ] 手工冒烟：
  - 在左栏 `+` -> `添加 AI解读`，填写标题/提示词/来源/间隔/分类，创建后左栏出现新条目
  - 点击该条目，中栏列表为空时工具栏显示 `立即生成`
  - 在窗口内有 RSS 新抓取文章时：点击 `立即生成`，应新增一篇解读文章；智能视图 `全部/未读/收藏` 不出现该文章
  - 点击 `全部文章` -> 一键已读，不应影响 AI解读源下未读计数

---

Plan complete and saved to `docs/superpowers/plans/2026-03-14-ai-digest.md`. Ready to execute?

