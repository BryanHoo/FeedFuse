# 背景
用户按 `docs/deploy.md` 部署后，`http` 访问无法登录，而 `https` 可登录。

# 目标
修复部署后的登录流程，使 `http` 与 `https` 场景均可完成登录。

# 范围
- 排查并修复导致 `http` 登录失败的根因。
- 必要时同步更新 `docs/deploy.md`，避免后续部署踩坑。

# 验收标准
- 在本地配置中模拟 `http` 环境，登录请求可成功并完成会话建立。
- `https` 登录行为不回归。
- 相关 lint / type-check / 测试通过。
